---
description: 开发任务走 Superpowers 工作流；前端/UI 任务走 frontend-design skill
alwaysApply: true
---

# Skill 路由规则

开始动手前，先判断任务类型，**必须先 Read 对应 SKILL.md 再执行**。

## 开发类任务 → Superpowers

适用于：功能实现、重构、Bug 修复、测试、架构调整、后端/主进程/协议/生成逻辑等。

1. 先读 `using-superpowers`（Superpowers 插件入口）：
   `C:\Users\19692\.cursor\plugins\cache\cursor-public\superpowers\b7a8f76985f1e93e75dd2f2a3b424dc731bd9d37\skills\using-superpowers\SKILL.md`
2. 按任务选用子 skill（同目录 `skills/` 下）：
   - 新功能 / 需求不清 → `brainstorming`
   - 多步实现 → `writing-plans` → `executing-plans`
   - Bug → `systematic-debugging`
   - 新逻辑 / 行为变更 → `test-driven-development`
   - 完成前 → `verification-before-completion`
   - 重要改动后 → `requesting-code-review`
3. 不得跳过 skill 直接写代码；用户明确指令优先于 skill 流程。

## 前端 / UI 类任务 → frontend-design

适用于：React 组件、设置页、桌宠窗口 UI、样式美化、布局与动效、视觉改版。

1. 先读：
   `C:\Users\19692\.agents\skills\frontend-design\SKILL.md`
2. 遵循其设计方向（字体、配色、动效、空间构图），避免通用 AI 审美。
3. 桌宠像素渲染（`sprite-canvas`、DSL）属开发逻辑，走 Superpowers；仅包裹它的 UI 壳走 frontend-design。

## 重叠时

先 Superpowers 定流程（调试/计划/TDD），再在 UI 实现阶段读 frontend-design。
